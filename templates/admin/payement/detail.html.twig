{% extends "base.admin.html.twig" %}

{% block title %}
	Detail du payement de
	{{ payement.eleve.nom }}
	{{ payement.eleve.prenom }}

{% endblock %}

{% block titre %}
	Detail du payement de
	{{ payement.eleve.nom }}
	{{ payement.eleve.prenom }}

{% endblock %}
{% block boutons %}
	<a href="{{ path("admin_payement_editer", {"id": payement.id}) }}" title="Editer cette scolarité" class="btn btn-inverse btn-actions btn-outline-inverse">
		<i class="ti-marker-alt"></i>
	</a>
	<a href="{{ path("admin_payement_supprimer", {"id": payement.id}) }}" title="Supprimer cette anneé scolaire" class="btn btn-danger btn-outline-danger" onclick="return confirm('Etes vous sur de vouloir supprimer cet scolarité ? Cette action est irreversible')">
		<i class="ti-trash"></i>
	</a>
{% endblock %}

{% block body %}
	<div class="card">
		<div class="card-body">
			<div class="content">
				<div class="container">
					<h3>Nom Prenom: {{ payement.eleve.nom }} {{ payement.eleve.prenom }}</h3>
					<h3>Payement: {{ payement.frais }}</h3>
					<h3>Date: {{payement.datePayement|date("m/d/Y")}}</h3>
				</div>
			</div>
		</div>
	</div>
	<a href="{{ path("admin_trimestre_annee") }}" class="mr-2 btn btn-inverse btn-outline-inverse float-left">
		Retour a la liste des Annees</i>
</a>
<br>{% endblock %}{% block javascripts %}{% endblock %}
