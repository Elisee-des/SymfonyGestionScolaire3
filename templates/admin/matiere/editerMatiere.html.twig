{% block content_titre_modale %}
	Editer la matiere {{ matiere.nom }}
{% endblock %}
<form method="POST" action="{{ path("admin_matiere_editer_matiere", {id: matiere.id}) }}">
	{{ form_widget(form) }}
</form>
